/**
 * @brief 单峰函数, 只有一个最大值, 在最大值左边严格单调递增, 在最大值右边严格单调递减
 * 
 * @param x 入参
 * @return double 函数的返回值
 */
double single_max_function(double x);

/**
 * @brief 单谷函数, 只有一个最小值, 在最小值左边严格单调递减, 在最小值右边严格单调递增
 * 
 * @param x 入参
 * @return double 函数的返回值
 */
double single_min_function(double x);